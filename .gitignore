# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — nunca commitar
.env
.env.local
.env.*.local
.env.bak*
.env.*.bak

# Supabase CLI cache (project ref, link metadata)
supabase/.temp/
supabase/.branches/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code settings
.claude/settings.local.json

# Imports avulsos do sistema antigo (dados locais — não commitar)
/Plano de Cargos.xlsx
scripts/output/

# Certificado do Santander — chave privada do e-CNPJ da empresa.
# Commitar isso é incidente de segurança, não descuido: o A1 assina em nome da
# Softcom e vale até 12/08/2027. Os PEM vivem só no volume da VPS.
deploy/certs/
santander-gw/certs/
*.pfx
*.p12
client.key
client.crt
CADEIA_COMPLETA.pem
